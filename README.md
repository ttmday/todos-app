# todos-app


```bash
#Actividad nro 1

# Forkear codigo a nuestro github

mkdir todo-app
cd todo-app

git init
git remote add origin <url-de-nuestro-repo>
git branch -M <nuestro-nombre>
git pull origin <nuestro-nombre>

# realizar cambios
git add .
git commit -m "nuestro mensaje"
git push origin <nuestra-nombre>
```
